"""
PROD/utils
==========
Utility modules for CHEWY BankNifty Options Trading system.

Modules:
    kite_helpers  — KiteConnect login, SparkSession factory
    strike_utils  — ATM strike calc, instrument schemas, parquet readers
    data_fetcher  — Historical OHLC fetch, Parquet write, scheduled loop
    chart_utils   — Analytics + Plotly chart rendering, auto-refresh loop
"""
